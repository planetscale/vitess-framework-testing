#!/bin/bash -ex
dir="$(dirname "${0}")"
pip3 install pyyaml
"${dir}/get-changed-files" # print them here for debugging purposes
buildkite-agent meta-data set 'frameworks' "$(./tools/get-frameworks < <("${dir}/get-changed-files"))"

